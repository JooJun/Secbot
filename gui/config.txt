#screen resolution
resolution_width = 800
resolution_height = 1200

#Pi IP address
#pi_ip = 192.168.1.2
pi_ip = 192.168.1.176

#Camera IP address
cam_ip = 192.168.1.10

#Pi username and password (this will be replaced with something secure)
pi_username = pi
pi_password = vadelma77

#Name/path of content folder locally example;- content or content/pictures/, trailing / is essential
content_folder = content/

#console file path and name (on the Pi)
console_file_path = /home/pi/Devel/secbot/files/main.log

#depthmap file paths and names (on the Pi and local copy)
depthmap_file_path_remote = /home/pi/Devel/secbot/files/depth.png
depthmap_file_path_local = depth/depth.png